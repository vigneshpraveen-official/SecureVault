spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/securevault
    username: postgres
    password: CHANGE_ME
  jpa:
    show-sql: true
  flyway:
    url: jdbc:postgresql://localhost:5432/securevault
    user: postgres
    password: CHANGE_ME

# Per-profile logger levels now live in logback-spring.xml (P4.7) — single source of truth,
# not duplicated here.
